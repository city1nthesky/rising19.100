#ifndef CONFIG_H
#define CONFIG_H

#define MAX_WIDTH   80
#define MAX_HEIGHT  24

//#ifdef CHINESE
//    #define STRING_LENGTH(x) (strlen(x)-strlen(x)/3)
//#else
    #define STRING_LENGTH(x) strlen(x)
//#endif

#endif // CONFIG_H
